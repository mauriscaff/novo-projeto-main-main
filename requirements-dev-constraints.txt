# Locked versions for reproducible dev/CI test environment.
# Install with:
#   pip install -r requirements-dev.txt
#
# Update policy:
# - edit ranges in requirements-dev.txt when needed
# - run test suite
# - update pins in this file in the same PR

pytest==9.0.2
pytest-asyncio==1.3.0
pytest-cov==7.0.0

# Transitive pins for stable pytest stack
coverage==7.11.3
iniconfig==2.1.0
packaging==25.0
pluggy==1.6.0
pygments==2.19.2
