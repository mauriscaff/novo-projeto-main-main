# Development/test dependencies (spec)
# Use with constraints for reproducible installs in local/CI.
# Install: pip install -r requirements-dev.txt

-c requirements-dev-constraints.txt
-r requirements.txt

# Test tooling ranges (updated in controlled PRs)
pytest>=9,<10
pytest-asyncio>=1.3,<2
pytest-cov>=7,<8
