# Testes automatizados — ZombieHunter
# Instalar: pip install -r requirements-dev.txt
#
# pytest          — runner e assertions
# pytest-asyncio  — suporte a testes async (FastAPI, init_db)
# pytest-cov      — cobertura de código
# httpx           — cliente HTTP para TestClient (FastAPI)
#
# unittest.mock  — stdlib, não precisa instalar

-r requirements.txt

# Pins para execucao reproduzivel em dev/CI
pytest==9.0.2
pytest-asyncio==1.3.0
pytest-cov==7.0.0
httpx==0.28.1
